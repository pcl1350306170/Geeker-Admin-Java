# 部署环境配置，运行 jar 包时通过 --spring.profiles.active=prod 激活
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/geeker_admin?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: dev_user
    password: 123456
